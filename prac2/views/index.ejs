<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD operations with Node.js and MongoDB - Prac2</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="../public/stylesheets/style.css">

</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-4">
    <div class="w-full max-w-xl bg-zinc-800 shadow-xl rounded-2xl p-8">
  
  
      <!-- Navigation -->
      <div class="mb-6 text-right">
        <a href="/read" class="text-blue-300 hover:underline hover:text-blue-400 transition">Read Users</a>
      </div>
  
      <!-- Heading -->
      <h1 class="text-3xl font-semibold mb-8 text-center tracking-wide">Create User</h1>
  
      <!-- Form -->
      <form action="/create" method="post" class="space-y-6">
        <div>
          <label class="block mb-2 text-sm text-gray-300">Name</label>
          <input
            type="text"
            name="name"
            placeholder="Enter full name"
            class="w-full px-4 py-3 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          />
        </div>
  
        <div>
          <label class="block mb-2 text-sm text-gray-300">Email</label>
          <input
            type="email"
            name="email"
            placeholder="Enter email address"
            class="w-full px-4 py-3 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
            required
          />
        </div>
  
        <div>
          <label class="block mb-2 text-sm text-gray-300">Image URL</label>
          <input
            type="text"
            name="image"
            placeholder="Enter image URL"
            class="w-full px-4 py-3 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
  
        <div class="text-center">
          <input
            type="submit"
            value="Create User"
            class="w-full sm:w-auto px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition duration-200 cursor-pointer"
          />
        </div>
      </form>
    </div>
  </body>
  
</html>