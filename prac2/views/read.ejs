<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD operations with Node.js and MongoDB - Prac2</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="../public/stylesheets/style.css">

</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white ">
        <div class="nav mb-10">
            <a href="/" class="text-blue-200">Back to home</a>
        </div>
        <h1 class="text-3xl mb-5 tracking-lighter">All Users</h1>

        <div class="users flex flex-wrap gap-5">

            <% if (users.length > 0) { %>
                <% users.forEach(user => { %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-52 rounded-lg bg-zinc-700 overflow-hidden">
                            <img src="<%= user.image %>" alt="" class="w-full h-full object-cover rounded-lg">
                        </div>
                        <h3 class="text-xl mt-3 text-center tracking-lighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500 mt-2 text-center tracking-lighter"><%= user.email %></h5>
                        <div class="flex gap-3 mt-10">
                            <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit USer</a>
                            <a class="text-red-600" href="/delete/<%= user._id %>">Delete USer</a>
                        </div>
                    </div>
                   <% }) %>
            <% } else {%>
                <h3 class="text-2xl">No users found</h3>
            <% } %>
           

        </div>
        
    </div>  
</body>
</html>