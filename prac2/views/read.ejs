<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CRUD operations with Node.js and MongoDB - Prac2</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel="stylesheet" href="../public/stylesheets/style.css" />
</head>
<body class="bg-zinc-900 text-white min-h-screen p-4 sm:p-8">
  <div class="max-w-7xl mx-auto">
    
    <!-- Navigation -->
    <div class="mb-8">
      <a href="/" class="text-blue-300 hover:text-blue-400 underline transition">← Back to Home</a>
    </div>

    <!-- Heading -->
    <h1 class="text-3xl font-semibold mb-6 tracking-wide text-center sm:text-left">All Users</h1>

    <!-- Users Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
      <% if (users.length > 0) { %>
        <% users.forEach(user => { %>
          <div class="bg-zinc-800 rounded-xl overflow-hidden shadow-lg transition-transform transform hover:-translate-y-1 hover:shadow-2xl">
            <div class="w-full h-52 bg-zinc-700 overflow-hidden">
              <img src="<%= user.image %>" alt="<%= user.name %>" class="w-full h-full object-cover" />
            </div>
            <div class="p-4 text-center">
              <h3 class="text-xl font-medium mb-1"><%= user.name %></h3>
              <p class="text-sm text-zinc-400 mb-4"><%= user.email %></p>
              <div class="flex justify-center gap-4">
                <a href="/edit/<%= user._id %>" class="px-3 py-1 bg-blue-600 hover:bg-blue-700 rounded text-white text-sm transition">Edit</a>
                <a href="/delete/<%= user._id %>" class="px-3 py-1 bg-red-600 hover:bg-red-700 rounded text-white text-sm transition">Delete</a>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-span-full text-center">
          <h3 class="text-2xl text-zinc-400">No users found</h3>
        </div>
      <% } %>
    </div>

  </div>
</body>
</html>
