<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit User</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel="stylesheet" href="../public/stylesheets/style.css" />
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-4">
  <div class="w-full max-w-xl bg-zinc-800 shadow-xl rounded-2xl p-8">

    <!-- Navigation -->
    <div class="mb-4 text-sm">
      <a href="/read" class="text-blue-300 hover:underline hover:text-blue-400 transition mr-4">← Read Users</a>
      <a href="/" class="text-blue-300 hover:underline hover:text-blue-400 transition">← Back to Home</a>
    </div>

    <!-- Heading -->
    <h1 class="text-3xl font-semibold mb-8 text-center tracking-wide">Edit User</h1>

    <!-- Form -->
    <form action="/update/<%= user._id %>" method="post" class="space-y-6">
      <div>
        <label class="block mb-2 text-sm text-gray-300">Name</label>
        <input
          type="text"
          name="name"
          placeholder="Enter name"
          value="<%= user.name %>"
          class="w-full px-4 py-3 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-yellow-400"
          required
        />
      </div>

      <div>
        <label class="block mb-2 text-sm text-gray-300">Email</label>
        <input
          type="email"
          name="email"
          placeholder="Enter email"
          value="<%= user.email %>"
          class="w-full px-4 py-3 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-yellow-400"
          required
        />
      </div>

      <div>
        <label class="block mb-2 text-sm text-gray-300">Image URL</label>
        <input
          type="text"
          name="image"
          placeholder="Enter image URL"
          value="<%= user.image %>"
          class="w-full px-4 py-3 rounded-lg bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-yellow-400"
        />
      </div>

      <div class="text-center">
        <input
          type="submit"
          value="Update User"
          class="w-full sm:w-auto px-6 py-3 bg-yellow-300 hover:bg-yellow-400 text-black font-semibold rounded-lg transition duration-200 cursor-pointer"
        />
      </div>
    </form>
  </div>
</body>
</html>
